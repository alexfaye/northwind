config {
    type: "table",
    name: "stg_strings",
    tags: ["supplier_performance"],
    description: "This is the stage db for strings."
}

select *, current_timestamp as ingestion_timestamp from ${ref("strings")}